version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: mongodb
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - /Users/yunjoolee/Desktop/ai-service/mongodb_data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=careguide
